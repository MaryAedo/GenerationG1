package com.generation.f20220530;

public class ApuntesAlumno {
	//atributos
	//private, protected or public; estos son los 3 tipos
		//private encapsula/protege la info porque 
		//no cualquiera puede tener acceso a esto;  
		//se tiene acceso dentro de la misma clase
		
		//a los protected pueden acceder todos 
		//los que estén dentro del mismo package
		
		//public, cualquiera accede desde donde sea
		
		//por lo general el atributo se genera en singular
		//cuando es matriz (por ej), se puede poner en plural
	
	//constructores
	//
	
	
	//***************************
	/* Diferencias clave entre la lista y ArrayList:
	 * Una de las diferencias más importantes entre List y
	 *  ArrayList es que la lista es una interfaz y ArrayList 
	 *  es una clase de colección estándar.
	 * La interfaz de lista amplía el marco de la Colección, 
	 * mientras que ArrayList extiende la clase AbstractList 
	 * e implementa las interfaces de lista.
	 * El espacio de nombres para la interfaz de lista es
	 *  System.Collection.Generic mientras que,
	 *  el espacio de nombres para ArrayList es System.Collection.
	 * La interfaz de lista crea una colección de elementos que 
	 * se almacenan en una secuencia y 
	 * se identifican o acceden mediante su número de índice. 
	 * Por otro lado, ArrayList crea una matriz de objetos 
	 * donde la matriz puede crecer dinámicamente cuando sea necesario.
*/

}
